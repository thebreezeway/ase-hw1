
<h1>所有文章</h1>
 
<table>
  <tr>
    <th>标题：</th>
  </tr>
<>
<% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    
    <td><%= link_to "Show", article %></td>
    <td><%= link_to "Edit", edit_article_path(article) %></td>
    <td><%= link_to "Remove", article, method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>
<% end %>
</table>
 
<br>
 
<%= link_to "新文章", new_article_path %>

